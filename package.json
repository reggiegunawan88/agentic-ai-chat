{
	"name": "agentic-ai",
	"type": "module",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "bun run --filter '*' dev",
		"format": "biome format --write .",
		"lint": "biome lint .",
		"build": "bun run --filter '*' build"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.10"
	}
}
